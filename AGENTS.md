# SmartJobTracker — agent context

Read `CLAUDE.md` in full before doing anything. Despite its name it is the tool-agnostic
context file for every AI coding agent on this project: architecture, the sync-engine rules
that are easy to break, conventions, commands and known issues. It is the single source of
truth — do not maintain a copy here, and update it in the same commit as any architecture
change.
